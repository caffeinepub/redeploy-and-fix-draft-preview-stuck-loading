export default function DataInitializer() {
  // Backend no longer supports portfolio data initialization
  // All data is now static and defined in frontend/src/data/portfolioData.ts
  return null;
}
